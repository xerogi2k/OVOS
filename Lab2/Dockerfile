# Используем базовый образ Node.js
FROM node:20

# Устанавливаем рабочую директорию
WORKDIR /app

# Копируем все содержимое папки "2048-game-master" в контейнер
COPY ./2048-game-master /app

# Открываем порт 8080 (или другой порт, на котором работает сервер)
EXPOSE 8080

# Определяем команду запуска сервера
ENTRYPOINT ["node", "server.js"]
